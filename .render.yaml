services:
  - type: web
    name: Eingabe-Matching-Anzeigen
    region: frankfurt
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: GITHUB_TOKEN
        sync: false


